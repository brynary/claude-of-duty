node_modules/
dist/
shots/
*.log
.DS_Store

runs/
# Every harness run gets its own profile directory, named with its pid: a
# shared one intermittently fails to launch when a stale lock outlives a
# killed Chrome.
.chrome-profile*/
